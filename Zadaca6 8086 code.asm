DATA SEGMENT
 P1 DW -1
 P2 DW -1
 N1 DW ?
 N2 DW ?
 VEK1 DW 1000h DUP (?)
 VEK2 DW 1000h DUP (?)
DATA ENDS
CODE SEGMENT
 ASSUME CS:CODE, DS:DATA
START: MOV AX,DATA 
 MOV DS,AX
 MOV DI,0 
 MOV BX,N2 
 VRTI_V2: MOV SI,0 
 MOV CX,N1 
 MOV AX,VEK2[DI] 
VRTI_V1: CMP AX,VEK1[SI] 
 JZ IZLEZ 
 ADD SI,2 
LOOP VRTI_V1
 ADD DI,2 
 DEC BX
 JNZ VRTI_V2
 HLT 
IZLEZ: SHR SI 
 SHR DI
 MOV P1,SI
 MOV P2,DI
 HLT
CODE ENDS
 END START
